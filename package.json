{
  "name": "gitbooks",
  "version": "1.0.0",
  "homepage": "https://cw0149.github.io/gitbooks/",
  "scripts": {
    "build": "./build.sh",
    "deploy-gh": "gh-pages -d build",
    "deploy": "source .env.local && npm run build && rm -rf $DEPLOY_PATH && mv build $DEPLOY_PATH"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
